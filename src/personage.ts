/* eslint-disable no-unused-expressions */
/* eslint-disable no-unused-vars */
export class Personage {
  constructor(
    public personageName: string,
    public lineage: string,
    public age: number
  ) {
    this.personageName = personageName;
    this.lineage = lineage;
    this.age;
  }
}
